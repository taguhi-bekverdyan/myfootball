<UserControl x:Class="MyFootballAdmin.Main.Views.Notifications.NotificationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyFootballAdmin.Main.Views.Notifications"
             xmlns:mvvm="http://prismlibrary.com/"
             mvvm:ViewModelLocator.AutoWireViewModel="True"             
             mc:Ignorable="d" 
               d:DataContext="{d:DesignInstance local:NotificationViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="40" d:DesignWidth="800">
    <Grid Background="{Binding Colour}">
        <Grid.RowDefinitions>
            <RowDefinition/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="15*"/>
        </Grid.ColumnDefinitions>
        <Line Grid.ColumnSpan="2" X1="0" X2="3800" Stroke="{Binding Colour}" />
        <TextBlock Foreground="White" Text="{Binding Message,Mode=TwoWay}" FontSize="13" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center"/>
    </Grid>
</UserControl>
