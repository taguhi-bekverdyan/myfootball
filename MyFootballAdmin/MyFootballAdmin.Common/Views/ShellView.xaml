<Window x:Class="MyFootballAdmin.Common.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyFootballAdmin.Common.Views"
        xmlns:mvvm="http://prismlibrary.com/"
        xmlns:common="clr-namespace:MyFootballAdmin.Common"
        mvvm:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:ShellViewModel, IsDesignTimeCreatable=True}"
        Title="{Binding Title}" Height="700" Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <ContentControl mvvm:RegionManager.RegionName="{x:Static common:RegionNames.MainRegion}"/>
        <ContentControl Grid.Row="1" mvvm:RegionManager.RegionName="{x:Static common:RegionNames.NotificationRegion}"/>
    </Grid>
</Window>
